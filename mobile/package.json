{
  "name": "somdul-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Android app shell (Capacitor) for สมดุล — wraps the repo-root frontend in a native WebView pointed at the hosted backend",
  "scripts": {
    "sync-web": "node scripts/sync-web.mjs",
    "sync": "npm run sync-web && cap sync android",
    "open:android": "cap open android",
    "build:apk": "npm run sync-web && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.2",
    "@capacitor/core": "^8.4.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.2"
  }
}
