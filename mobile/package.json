{
  "name": "somdul-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Android app shell (Capacitor) for เงินทอง — wraps frontend/ in a native WebView pointed at the hosted backend",
  "scripts": {
    "sync": "cap sync android",
    "open:android": "cap open android",
    "build:apk": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.2",
    "@capacitor/core": "^8.4.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.2"
  }
}
